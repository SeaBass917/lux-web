import "./HorizontalScrollSection.css";

function HorizontalScrollSection({ title }) {
  return (
    <div className="HorizontalScrollSection">
      <h1>{title}</h1>
    </div>
  );
}

export default HorizontalScrollSection;
